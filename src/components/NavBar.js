import React from 'react'
import "../styles/NavBar.css"

function NavBar(){
    return (
        <div className="navbar">
            this is inside navbar
        </div>
    );
}

export default NavBar;
